你是一个经验丰富的软件工程师，现在需要帮我从零设计并实现一个“考研错题复习提醒系统”。

项目目标：
我要做一个用于考研学习复习的本地程序，主要功能是记录错题、管理错题章节、生成每日复习任务，并根据完成情况自动延期。该程序服务于我的考研复习，科目包括：

专业课：
1. 数字电子技术

数学课：
1. 高等数学
2. 线性代数
3. 概率论

如果当前目录下已经有项目，请先阅读已有工程结构并适配；如果当前目录为空，请从零创建项目。优先技术栈建议使用：
- Python
- SQLite
- Streamlit
- pandas 可选

如果你认为有更合适的技术栈，可以先说明理由，但不要使用过度复杂的前后端分离架构。该项目应以“本地可运行、数据可持久化、界面简单清晰、后续方便扩展”为原则。

一、核心功能需求

1. 科目与章节管理

系统内置以下科目：
- 数字电子技术
- 高等数学
- 线性代数
- 概率论

每个科目下的错题需要按照章节划分。章节可以由用户手动输入，例如：
- 高等数学 / 极限
- 高等数学 / 一元函数微分学
- 线性代数 / 矩阵
- 概率论 / 随机变量
- 数字电子技术 / 组合逻辑电路
- 数字电子技术 / 时序逻辑电路

要求：
- 支持新增章节。
- 支持按科目筛选章节。
- 支持按章节查看错题。
- 不要求一开始预置完整章节目录，但数据结构要支持后续扩展。

2. 错题录入功能

系统需要支持两种录入方式：

方式一：录入历史错题
用于我把以前积累的错题批量录入系统。

方式二：录入每日错题
用于我每天学习结束后，输入当天新产生的错题。

每一道错题至少包含以下字段：
- 题目标题
- 所属科目
- 所属章节
- 题目内容
- 我的错误原因
- 正确解法 / 解析
- 关键知识点
- 错题来源，例如：真题、模拟题、习题册、课堂、自己整理
- 难度等级，可选：简单、中等、困难
- 是否收藏
- 是否熟练
- 创建日期
- 最近复习日期
- 下次计划复习日期
- 复习次数
- 错误次数
- 备注

要求：
- 支持新增错题。
- 支持编辑错题。
- 支持删除错题。
- 支持查看错题详情。
- 支持按科目、章节、收藏状态、熟练状态、关键词搜索。
- 支持标记收藏。
- 支持取消收藏。
- 支持标记熟练。
- 支持取消熟练。

3. 每日复习任务生成

每日每个科目需要复习 5 个错题。

也就是说，每天理论上需要生成：
- 数字电子技术：5 题
- 高等数学：5 题
- 线性代数：5 题
- 概率论：5 题

总计最多 20 题。

如果某个科目错题数量不足 5 题，则显示当前可复习的全部错题，不强行补足。

复习任务选择规则要求：

优先级从高到低：
1. 昨日或之前没有完成的延期错题。
2. 已收藏的错题。
3. 到达下次复习日期的普通错题。
4. 复习次数较少的错题。
5. 最近较久没有复习的错题。

熟练题目的规则：
- 如果一道错题被标记为“熟练”，则默认不再自动出现在每日复习任务中。
- 但是用户可以在筛选中手动查看熟练题。
- 如果用户主动取消熟练，则该题重新进入复习池。
- 收藏题即使复习过，也应具有更高优先级，便于重点复习。
- 如果一道题同时被标记为“收藏”和“熟练”，默认以“熟练”为准，不自动提醒；但需要在界面中明确显示这种状态，避免用户误解。

4. 复习完成与延期逻辑

每日任务中，每一道题需要支持以下操作：
- 标记已完成
- 标记未完成
- 标记仍然错误
- 标记已经掌握
- 收藏 / 取消收藏
- 熟练 / 取消熟练

任务延期规则：
- 如果当天某道题没有完成，则自动延期到下一天。
- 延期题目在第二天生成任务时优先出现。
- 如果连续多天未完成，需要继续保留在待复习任务中。
- 延期题不应该被新生成的普通复习题覆盖。
- 每个科目每天仍然以 5 题为目标，但延期题优先占用名额。

例如：
今天高等数学生成了 5 题，我只完成了 3 题，剩余 2 题没有完成。
那么明天高等数学的 5 题中，应优先包含昨天没完成的 2 题，再补充 3 道新的复习题。

5. 复习记录

系统需要记录每次复习行为。每条复习记录至少包括：
- 错题 ID
- 复习日期
- 是否完成
- 是否答对
- 是否仍然错误
- 是否标记熟练
- 当天备注

要求：
- 可以查看某道题的历史复习记录。
- 可以统计每个科目总错题数、已熟练数、收藏数、今日待复习数、今日已完成数。
- 可以查看最近 7 天或 30 天的复习完成情况。

6. 首页 Dashboard

程序首页需要显示：
- 今日日期
- 今日总复习任务数
- 今日已完成数量
- 今日未完成数量
- 各科目今日任务进度
- 各科目错题总数
- 收藏题数量
- 熟练题数量
- 延期题数量
- 一个“生成今日复习任务”按钮
- 一个“进入今日复习”按钮
- 一个“录入新错题”按钮

7. 页面设计

建议至少包含以下页面：

1. 首页 Dashboard
2. 今日复习
3. 新增错题
4. 错题库
5. 章节管理
6. 复习记录
7. 数据统计
8. 数据导入导出

8. 数据导入导出

需要支持：
- 导出全部错题为 CSV
- 从 CSV 导入历史错题
- 导出复习记录为 CSV
- 备份 SQLite 数据库

导入历史错题时，如果字段不完整，需要给出提示，不要导致程序崩溃。

二、数据模型要求

请使用 SQLite 设计数据表。至少包含：

1. subjects 表
用于保存科目信息。

字段建议：
- id
- name
- category，例如：专业课、数学课

2. chapters 表
用于保存章节信息。

字段建议：
- id
- subject_id
- name
- description
- created_at

3. mistakes 表
用于保存错题主体。

字段建议：
- id
- subject_id
- chapter_id
- title
- content
- wrong_reason
- solution
- knowledge_points
- source
- difficulty
- is_favorite
- is_mastered
- created_at
- updated_at
- last_review_date
- next_review_date
- review_count
- wrong_count
- note

4. daily_tasks 表
用于保存每日复习任务。

字段建议：
- id
- task_date
- mistake_id
- subject_id
- status，例如 pending、done、postponed
- is_completed
- is_correct
- created_at
- completed_at

5. review_records 表
用于保存复习记录。

字段建议：
- id
- mistake_id
- review_date
- is_completed
- is_correct
- marked_mastered
- note
- created_at

三、复习任务生成算法

请实现一个函数，例如：

generate_daily_tasks(date)

基本逻辑：

1. 对每个科目分别生成任务。
2. 每个科目目标数量为 5。
3. 先查询该科目历史未完成且未熟练的延期题。
4. 再查询该科目收藏且未熟练的题。
5. 再查询该科目 next_review_date <= today 且未熟练的题。
6. 再查询复习次数少、最近复习时间较久的题。
7. 去重。
8. 按优先级选满 5 题。
9. 如果不足 5 题，则有多少生成多少。
10. 如果当天已经生成过任务，则不要重复生成；可以提示用户“今日任务已存在”，或者提供“重新生成”按钮。
11. 重新生成任务时，不应删除已经完成的任务，除非用户明确确认。

四、代码结构要求

如果从零开始创建项目，请建议采用如下结构：

exam_review_app/
├── app.py
├── requirements.txt
├── README.md
├── data/
│   └── review.db
├── src/
│   ├── db.py
│   ├── models.py
│   ├── services.py
│   ├── task_scheduler.py
│   ├── import_export.py
│   └── utils.py
└── pages/
    ├── 1_今日复习.py
    ├── 2_新增错题.py
    ├── 3_错题库.py
    ├── 4_章节管理.py
    ├── 5_复习记录.py
    ├── 6_数据统计.py
    └── 7_数据导入导出.py

要求：
- 代码结构清晰。
- 数据库操作封装，不要全部写在 app.py。
- 关键函数加中文注释。
- 界面文字使用中文。
- README.md 中写清楚如何安装、如何运行、如何备份数据。
- requirements.txt 中列出依赖。

五、实现步骤要求

请不要一上来直接写大量代码。请按以下顺序执行：

第一步：分析需求
- 总结你理解的功能需求。
- 指出是否存在歧义。
- 在不影响开发的情况下，给出默认假设。
- 不要反复问我，除非确实无法继续。

第二步：设计方案
- 给出整体架构。
- 给出数据表设计。
- 给出每日任务生成算法。
- 给出页面结构。
- 给出文件结构。

第三步：实现最小可用版本 MVP
MVP 必须支持：
- 初始化数据库
- 内置四个科目
- 新增章节
- 新增错题
- 查看错题库
- 标记收藏
- 标记熟练
- 生成今日任务
- 每个科目最多生成 5 道题
- 未完成任务延期
- 今日复习页面
- 标记完成
- 简单统计

第四步：本地运行测试
- 创建测试数据。
- 运行程序。
- 检查数据库是否正确初始化。
- 检查每日任务生成是否正确。
- 检查熟练题是否不再自动提醒。
- 检查收藏题是否优先出现。
- 检查未完成题是否延期。
- 检查 CSV 导入导出是否可用。

第五步：完善文档
- 生成 README.md。
- 说明使用方法。
- 说明数据备份方法。
- 说明后续可以扩展的功能。

六、界面要求

界面不需要花哨，但要清晰实用。

首页需要显示：
- 今日任务总数
- 今日完成数
- 今日未完成数
- 各科目进度
- 生成今日任务按钮

今日复习页面需要：
- 按科目分组显示今日错题
- 展示题目标题、章节、难度、是否收藏、是否熟练
- 支持展开查看题目内容和解析
- 支持标记完成
- 支持标记仍然错误
- 支持收藏/取消收藏
- 支持熟练/取消熟练

错题库页面需要：
- 支持按科目筛选
- 支持按章节筛选
- 支持按收藏筛选
- 支持按熟练筛选
- 支持关键词搜索
- 支持编辑和删除

七、注意事项

1. 不要把数据只保存在内存里，必须持久化到 SQLite。
2. 不要用复杂登录系统，先做本地单用户版本。
3. 不要依赖云服务。
4. 不要过度设计，不要引入大型后端框架。
5. 不要使用过度复杂的前端工程。
6. 不要因为没有历史数据就报错。
7. 对空数据状态要友好提示。
8. 对导入 CSV 的异常要做处理。
9. 所有日期统一使用 YYYY-MM-DD。
10. 每次生成任务要避免重复添加同一天同一道题。
11. 熟练题默认不进入每日复习池。
12. 延期题优先级最高。
13. 收藏题优先级高于普通题。
14. 如果某个科目不足 5 题，不要跨科目补题。

八、最终交付物

请最终生成：
1. 可运行的 Streamlit 程序。
2. SQLite 数据库初始化逻辑。
3. requirements.txt。
4. README.md。
5. 基本测试数据或测试脚本。
6. 简要开发说明。

九、运行方式

请在 README.md 中写明类似：

pip install -r requirements.txt
streamlit run app.py

十、当前请先执行

请先不要直接写代码。先输出：
1. 需求理解。
2. 默认假设。
3. 数据库表设计。
4. 页面设计。
5. 任务生成算法。
6. 文件结构。
7. MVP 实现计划。

补充需求一：每日复习题目需要尽量分散在不同章节

每日生成复习任务时，不能只按优先级简单选取前 5 道题。需要在每个科目内部尽量让题目分散到不同章节，避免同一天某个科目的 5 道题全部来自同一章。

规则如下：

1. 每个科目每日目标仍然是 5 道题。
2. 在满足延期题优先、收藏题优先、到期题优先的前提下，尽量进行章节分散。
3. 生成某一科目的每日任务时，应优先从不同章节各选 1 道题。
4. 如果该科目可复习章节数大于等于 5，则尽量选择 5 个不同章节的题目。
5. 如果该科目可复习章节数不足 5，则允许部分章节重复，但应尽量均衡。
6. 延期题优先级最高。如果延期题集中在同一章节，可以先保证延期题被安排，再用剩余名额做章节分散。
7. 收藏题优先级较高，但也应尽量避免同一章节收藏题过度集中。
8. 熟练题默认仍然不进入每日任务池。
9. 不允许为了章节分散而跨科目补题。例如高等数学不足 5 题，不从线性代数补题。
10. 如果某个科目只有一个章节有错题，则可以全部从该章节生成。

建议实现方式：

在 generate_daily_tasks(date) 中，对每个科目单独生成候选题池。候选题先按优先级分层：

第一层：延期未完成题
第二层：收藏且未熟练题
第三层：next_review_date <= today 的普通到期题
第四层：复习次数少、最近较久未复习的普通题

在每一层内部，不要简单按列表顺序取题，而是按 chapter_id 做分组，然后使用轮询方式选择：

例如：
chapter A: 题1、题2、题3
chapter B: 题4、题5
chapter C: 题6

选择顺序尽量为：
A 选 1 道，B 选 1 道，C 选 1 道，然后再回到 A/B/C 补足数量。

这样可以保证同一科目的每日复习题目尽量覆盖更多章节。

需要在代码中把“章节分散选择逻辑”封装成单独函数，例如：

select_balanced_by_chapter(candidates, limit=5)

该函数输入某一科目的候选错题列表，输出尽量章节均衡的错题列表。

补充需求二：增加总复习进度查看页面

系统需要提供一个页面，用于查看“总的需要复习的题目”和“已经复习的题目”。

请新增或完善“数据统计 / 总览”页面，显示以下信息：

1. 总错题数
统计系统中全部错题数量。

2. 当前需要复习的题目数
统计所有未熟练、且符合以下条件之一的题目：
- 今日任务中待完成的题目
- 历史延期未完成的题目
- next_review_date <= today 的到期题
- 收藏且未熟练的重点题

3. 已经复习过的题目数
统计 review_count > 0 的错题数量。

4. 从未复习过的题目数
统计 review_count = 0 的错题数量。

5. 已熟练题目数
统计 is_mastered = 1 的题目数量。

6. 收藏题目数
统计 is_favorite = 1 的题目数量。

7. 今日需要复习题目数
统计 daily_tasks 中今日 pending / postponed 的任务数量。

8. 今日已经完成题目数
统计 daily_tasks 中今日 done 的任务数量。

9. 各科目复习进度
按科目显示：
- 总错题数
- 需要复习数
- 已复习数
- 已熟练数
- 收藏数
- 今日任务数
- 今日完成数

10. 各章节复习进度
按“科目 + 章节”显示：
- 章节总错题数
- 已复习数
- 未复习数
- 已熟练数
- 收藏数
- 当前待复习数

11. 支持筛选
该页面需要支持：
- 按科目筛选
- 按章节筛选
- 按收藏状态筛选
- 按熟练状态筛选
- 按是否到期筛选
- 按是否已复习筛选

12. 支持查看题目列表
在统计数字下面，需要能够展开查看对应题目列表。例如点击或选择“当前需要复习题目”，可以看到这些题目的：
- 题目标题
- 科目
- 章节
- 难度
- 是否收藏
- 是否熟练
- 最近复习日期
- 下次复习日期
- 复习次数
- 错误次数

13. 首页 Dashboard 也需要显示简要总览
首页至少显示：
- 总错题数
- 当前需要复习题目数
- 已经复习过的题目数
- 已熟练题目数
- 今日待完成题目数
- 今日已完成题目数

详细数据放在“数据统计 / 总览”页面中。

补充数据库/服务层要求：

请在 services.py 或 statistics.py 中实现统计函数，例如：

get_total_statistics()
get_subject_statistics()
get_chapter_statistics()
get_due_mistakes(today)
get_reviewed_mistakes()
get_unreviewed_mistakes()

不要把所有 SQL 查询都堆在 Streamlit 页面文件里。

补充验证要求：

测试时需要验证：
1. 每日每科最多生成 5 道题。
2. 同一科目有多个章节时，生成结果尽量分散在不同章节。
3. 延期题优先级高于章节分散。
4. 熟练题不会进入自动任务。
5. 收藏题优先级高于普通题。
6. 总览页面能正确显示：
   - 总错题数
   - 当前需要复习数
   - 已复习数
   - 未复习数
   - 已熟练数
   - 今日任务完成情况
7. 按科目和章节筛选统计结果时，数字应正确变化。

所有错题、任务、复习记录、收藏状态、熟练状态必须持久化保存到 SQLite 数据库中。程序关闭或电脑重启后，数据不能丢失。启动程序时，如果数据库已存在，应直接读取原数据库；只有在数据库不存在时才初始化新数据库。

等我确认后，再开始创建文件和实现代码。